"""Level -1 Auto-Fix System package.

Policy storage only -- the subgraph implementation remains in
subgraphs/level_minus1.py.  This package holds level-specific
policies/ and (future) helper modules.
"""
